// Public barrel for the right panel (Phase 1D / Task 7). Task 8 mounts this
// as `<RightPanel/>` inside ConsoleChrome's `rightPanel` slot:
// `import RightPanel from '@/modules/console/panels'`.

export { default } from './RightPanel'
export type { RightPanelProps } from './RightPanel'
