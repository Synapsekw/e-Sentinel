dist
package-lock.json

# Generated by tools/bake-flights.mjs: the DJI catalog and the AES keychains
# that decrypt each log's frames, alongside the raw .txt logs themselves.
# Prettier does not gate the style of files a build tool owns -- same reason
# dist and package-lock.json are here.
public/flights/
